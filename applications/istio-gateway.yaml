apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: istio-gateway
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/your-org/my-k8s  # set your git repo URL
    path: infrastructure/istio-gateway
    targetRevision: HEAD
  destination:
    server: https://kubernetes.default.svc
    namespace: george-app
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
